#include <stdio.h>

int main(){

    // char arr[] = {'H', 'E', 'L', 'L', 'O'};
    // char arr[] = "Hello";
    // printf("%s\n", arr);
    // printf("%c", arr[2]);

    // char str[15] = "Hello";
    // printf("%s", str);
    // printf("\0");

    // char ch = 'H'; //! Every Character represent an Integer constant value (ASCII)
    // char str[] = "H"; //! String returns pointer to the first character

    // int a = 10;
    // printf(a, "%d");

    // char str[] = "Hello";
    // int a = 10;
    // printf(str, "%s");
    // printf(a);

    // int a = 'H';
    // printf("%d", a);

    // char str[] = "Hello";
    // printf("%s", str);
    // puts(str);
    // puts(str);

    // char str[10];
    // printf("Enter the string: ");
    // scanf("%s", str);
    // gets(str);
    // fgets(str, sizeof(str), stdin);

    // printf("%s", str);
    // printf("%s", str);

    return 0;
}